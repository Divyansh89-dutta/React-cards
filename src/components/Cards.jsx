// import React from 'react';
// import Card from './Card';

// function Cards({ users, removeUser }) {
//   return (
//     <div className="w-full h-[50vh] flex justify-center flex-wrap gap-4 bg-sky-200 p-4">
//       {users.map((user, index) => (
//         <Card key={index} user={user} removeUser={() => removeUser(index)} />
//       ))}
//     </div>
//   );
// }

// export default Cards;
